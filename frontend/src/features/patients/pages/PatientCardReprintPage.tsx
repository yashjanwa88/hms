import React from 'react';
import { PatientCardReprint } from '../components/PatientCardReprint';

export const PatientCardReprintPage: React.FC = () => {
  return (
    <div className="container mx-auto p-6">
      <PatientCardReprint />
    </div>
  );
};
